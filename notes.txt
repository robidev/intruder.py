goal for virus is to propagate, extract a file, and send it to an ip.
  
javascript: generate tabs, based on pushing of data, 
  TODO write init.py script
  TODO send signal to forwarder re-init loggin (tail and such)

TODO level selector+artwork, will spin up machines and network

install:(windows/mac/linux)

  prerequisite:
  install docker

  bootstrap:
  start.sh: docker run -v /var/run/docker.sock:/var/run/docker.sock
  start.bat: -v //./pipe/docker_engine://./pipe/docker_engine

docker-compose build  - initialise/install/build (or build via docker?)
