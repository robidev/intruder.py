goal for virus is to propagate, extract a file, and send it to an ip.

TODO level selector, will spin up machines and network

webui:(flask)
 TODO upload to container via ssh(only 1 exposed to host) (pyinstaller)
 TODO issue get to webui/api with file contents
  
javascript: generate tabs, based on pushing of data, TODO remembering state
 TODO display pc-color(blue/red) based on log-message, gray if stale data
 TODO add connecting-lines based on connections(logging/subnet/netstat/scan?)

install:(windows/mac/linux)

  prerequisite:
  install docker

  bootstrap:
  start.sh: docker run -v /var/run/docker.sock:/var/run/docker.sock
  start.bat: -v //./pipe/docker_engine://./pipe/docker_engine

docker-compose build  - initialise/install/build (or build via docker?)
